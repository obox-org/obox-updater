{
  "name": "obox-updater-ext",
  "version": "1.0.0",
  "private": true,
  "description": "Obox 更新提供者扩展：从 GitHub Release（obox-org/obox）拉取并安装 obox 更新。",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "pack": "node scripts/pack.mjs",
    "check:feed": "node scripts/check-feed.mjs",
    "release": "npm run test && npm run pack"
  },
  "devDependencies": {
    "adm-zip": "^0.6.0"
  }
}
